<form method="post" action="{{ path('app_livraison1_delete', {'idLivraison': livraison1.idLivraison}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison1.idLivraison) }}">
    <button class="btn">Delete</button>
</form>

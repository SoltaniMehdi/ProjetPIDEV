

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
<div class="form-group{% if form_errors(form.categevent)|length > 0 %} has-error{% endif %}">
    {{ form_row(form.categevent, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.categevent)|length > 0 %}
        <span class="help-block">{{ form_errors(form.categevent)|first }}</span>
    {% endif %}
</div>


{# Other form fields... #}

<button type="submit" class="btn btn-primary">Submit</button>
{{ form_end(form) }}